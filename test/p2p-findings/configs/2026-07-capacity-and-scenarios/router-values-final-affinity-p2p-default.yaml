## p2p-kv-cache-sharing guide overrides for the router.
## Layer on top of: recipes/router/base.values.yaml
##
## Tokenization is served by a standalone `vllm launch render` Service deployed
## from `../render/`. The EPP `token-producer` plugin tokenizes prompts over
## HTTP against that Service (see its `vllm.url` below). Size the render pool
## for the peak request rate (long-prompt workloads saturate a single replica
## near ~10 req/s) - see the guide's Best Practices.
##
## The deployed EPP config is the affinity + P2P arm (`epp-affinity-p2p.yaml`
## in ../benchmarking/): placement follows the cache (prefix-affinity
## scoring), and the p2p-source-producer emits the KV cache source header
## when a peer out-caches the scheduled pod by at least minCachedTokenDelta
## tokens, so the pull covers what affinity alone misses. This guide
## measured two regimes on its own testbed and they point different ways:
## on the uniform shared-prefix pool this arm tracks offered rate to
## saturation and matches affinity's near-ideal latency, while the
## load-aware + P2P arm (`epp-load-p2p.yaml`) degrades badly at the top of
## the ladder (p50 30s at 24 req/s vs this arm's 0.82s); on the
## document-Q&A headline (128 concurrent sessions against a fixed
## per-document ownership rule) it is the other way - load-aware + P2P
## avoids the owner-pod queueing this arm still pays. This arm is the
## safer general-purpose default: close to affinity's ceiling in the
## capacity-driven regime, and still a clear improvement over affinity
## alone in the concurrency-driven one. Reach for `epp-load-p2p.yaml`
## specifically when your workload looks like many concurrent, multi-turn
## sessions each pinned to an owner pod - see
## [benchmark-results/gpt-oss-120b-h200.md](../benchmark-results/gpt-oss-120b-h200.md)
## for both measurements. To reproduce the comparison arms, swap this
## pluginsCustomConfig for any file under ../benchmarking/.
##
## Pod-discovery mode with active-active HA (replicas: 2). Each replica dials
## every vLLM pod's per-pod ZMQ socket; both indexes converge identically.

router:
  epp:
    replicas: 2
    # TEMPORARY pin, tracked under the v0.9 "Multi-tier offloading: NIXL P/D +
    # P2P backends" goal: the chart's default EPP image (the chart version's
    # tag) predates the `p2p-source-producer` plugin and rejects this config
    # with "plugin type 'p2p-source-producer' is not registered". Pin the next
    # release once it ships the plugin.
    image:
      tag: main
      pullPolicy: Always
    flags:
      ha-enable-leader-election: false
    env:
      - name: HF_TOKEN
        valueFrom:
          secretKeyRef:
            name: llm-d-hf-token
            key: HF_TOKEN
    pluginsConfigFile: "p2p-kv-cache-sharing-plugins.yaml"
    pluginsCustomConfig:
      p2p-kv-cache-sharing-plugins.yaml: |
        apiVersion: llm-d.ai/v1alpha1
        kind: EndpointPickerConfig
        plugins:
          - type: token-producer
            parameters:
              modelName: openai/gpt-oss-120b # Must match the render Service model (../render/) and the deployed model server
              vllm:
                # Standalone render Service (../render/): `p2p-kv-cache-sharing-`
                # namePrefix + Service name `render`.
                url: "http://p2p-kv-cache-sharing-render:8000"
          - type: endpoint-notification-source
          - type: precise-prefix-cache-producer
            parameters:
              tokenProcessorConfig:
                # MUST match the model server `--block-size`.
                blockSize: 64
              kvEventsConfig:
                topicFilter: "kv@"
                discoverPods: true
                podDiscoveryConfig:
                  socketPort: 5556
          - type: p2p-source-producer
            parameters:
              prefixMatchInfoProducerName: precise-prefix-cache-producer
              prefillProfileName: default
              # Minimum extra cached-prefix tokens a peer must hold beyond the
              # scheduled pod before a pull is requested; set from the measured
              # pull-versus-recompute crossover (see ../benchmarking/README.md).
              minCachedTokenDelta: 2048
          - type: prefix-cache-scorer
            parameters:
              prefixMatchInfoProducerName: precise-prefix-cache-producer
          - type: no-hit-lru-scorer
            parameters:
              prefixMatchInfoProducerName: precise-prefix-cache-producer
          - type: queue-scorer
          - type: kv-cache-utilization-scorer
          - type: max-score-picker
          - type: single-profile-handler
        dataLayer:
          sources:
            - pluginRef: endpoint-notification-source
              extractors:
                - pluginRef: precise-prefix-cache-producer
        schedulingProfiles:
          - name: default
            plugins:
              - pluginRef: max-score-picker
              - pluginRef: prefix-cache-scorer
                weight: 3
              - pluginRef: queue-scorer
                weight: 2
              - pluginRef: kv-cache-utilization-scorer
                weight: 2
              - pluginRef: no-hit-lru-scorer
                weight: 2

  modelServers:
    matchLabels:
      llm-d.ai/guide: "p2p-kv-cache-sharing"
