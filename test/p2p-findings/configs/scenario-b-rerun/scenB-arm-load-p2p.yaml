# Scenario B (hot set) re-validation, arm: load + P2P - the guide table's winning arm
# Plugin body is guides/p2p-kv-cache-sharing/benchmarking/epp-load-p2p.yaml verbatim,
# wrapped in the helm values scaffold and pointed at the scend-agg pods.
router:
  epp:
    image: { tag: main, pullPolicy: Always }
    pluginsConfigFile: "sb3.yaml"
    pluginsCustomConfig:
      sb3.yaml: |
        apiVersion: llm-d.ai/v1alpha1
        kind: EndpointPickerConfig
        # P2P arm: load-balanced placement + the pull. The p2p-source-producer
        # emits the KV cache source header when a peer out-caches the scheduled
        # pod by at least minCachedTokenDelta tokens (set from the measured
        # pull-versus-recompute crossover). Match modelName and the render
        # Service URL to your deployment.
        plugins:
        - type: token-producer
          parameters:
            modelName: openai/gpt-oss-120b
            vllm:
              url: http://gptoss-render:8000
        - type: endpoint-notification-source
        - type: metrics-data-source
          parameters: {scheme: "http", path: "/metrics", insecureSkipVerify: true}
        - type: core-metrics-extractor
        - type: precise-prefix-cache-producer
          parameters:
            tokenProcessorConfig:
              blockSize: 64
            kvEventsConfig:
              topicFilter: "kv@"
              discoverPods: true
              podDiscoveryConfig:
                socketPort: 5556
        - type: p2p-source-producer
          parameters:
            prefixMatchInfoProducerName: precise-prefix-cache-producer
            prefillProfileName: default
            minCachedTokenDelta: 2048
        - type: queue-scorer
        - type: kv-cache-utilization-scorer
        - type: weighted-random-picker
        - type: single-profile-handler
        dataLayer:
          sources:
          - pluginRef: metrics-data-source
            extractors:
            - pluginRef: core-metrics-extractor
          - pluginRef: endpoint-notification-source
            extractors:
            - pluginRef: precise-prefix-cache-producer
        schedulingProfiles:
        - name: default
          plugins:
          - pluginRef: weighted-random-picker
          - pluginRef: queue-scorer
            weight: 3
          - pluginRef: kv-cache-utilization-scorer
            weight: 2
  modelServers:
    matchLabels: { llm-d.ai/uc: scend }
